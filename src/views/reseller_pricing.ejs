<!DOCTYPE html>
<html lang="nl">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1">
    <title>Reseller Prijslijst - <%= formatDate(new Date()) %></title>
    <style>
        @media print {
            @page {
                size: A4 landscape;
                margin: 0;
            }
            body {
                -webkit-print-color-adjust: exact !important;
                print-color-adjust: exact !important;
            }
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, 'Helvetica Neue', Arial, sans-serif;
            line-height: 1.3;
            color: #333;
            background: white;
            font-size: 9px;
        }

        .page {
            width: 297mm;
            min-height: 210mm;
            padding: 8mm 10mm;
            box-sizing: border-box;
            background: white;
        }

        /* Header */
        .header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 8px;
            padding-bottom: 6px;
            border-bottom: 2px solid #214b53;
        }

        .header-left {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .logo {
            height: 30px;
            width: auto;
        }

        .header-title {
            font-size: 20px;
            color: #214b53;
            font-weight: 600;
        }

        .header-subtitle {
            font-size: 10px;
            color: #666;
        }

        .header-right {
            text-align: right;
            font-size: 9px;
            color: #666;
        }

        .header-date {
            font-weight: 600;
            color: #214b53;
            font-size: 11px;
        }

        /* Tables */
        .pricing-table {
            width: 100%;
            border-collapse: collapse;
            font-size: 8px;
        }

        .pricing-table thead th {
            padding: 4px 3px;
            text-align: center;
            font-weight: 600;
            color: white;
            background: #214b53;
            border: 1px solid #1a3d44;
            font-size: 8px;
        }

        .pricing-table thead th.qty-header {
            text-align: left;
            width: 50px;
            background: #1a3d44;
        }

        .pricing-table thead th.group-header {
            background: #2d6a76;
            font-size: 9px;
        }

        .pricing-table thead th.product-header {
            background: #3d8a96;
            font-size: 7px;
            font-weight: 500;
        }

        .pricing-table tbody tr {
            border-bottom: 1px solid #e0e0e0;
        }

        .pricing-table tbody tr:nth-child(even) {
            background: #f8f8f8;
        }

        .pricing-table td {
            padding: 2px 3px;
            text-align: center;
            border-left: 1px solid #e0e0e0;
            vertical-align: top;
        }

        .pricing-table td:first-child {
            border-left: 1px solid #ccc;
        }

        .pricing-table td:last-child {
            border-right: 1px solid #ccc;
        }

        .pricing-table td.qty-cell {
            text-align: left;
            font-weight: 600;
            color: #214b53;
            background: #f0f0f0;
            padding-left: 5px;
            vertical-align: middle;
        }

        .pricing-table td.price-cell {
            font-family: 'SF Mono', 'Monaco', 'Consolas', monospace;
            font-size: 7px;
            line-height: 1.4;
        }

        .pricing-table td.group-separator {
            border-left: 2px solid #214b53;
        }

        .price-row {
            display: block;
            white-space: nowrap;
        }

        .price-label {
            color: #888;
            font-size: 6px;
            display: inline-block;
            width: 28px;
            text-align: left;
        }

        .price-value {
            font-weight: 500;
        }

        .price-value.reseller {
            color: #214b53;
        }

        .price-value.retail {
            color: #2d8a4e;
            font-weight: 600;
        }

        /* Legend */
        .legend {
            display: flex;
            gap: 20px;
            margin-top: 8px;
            padding: 6px 10px;
            background: #f5f5f5;
            border-radius: 3px;
            font-size: 8px;
        }

        .legend-item {
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .legend-color {
            width: 10px;
            height: 10px;
            border-radius: 2px;
        }

        .legend-color.reseller {
            background: #214b53;
        }

        .legend-color.retail {
            background: #2d8a4e;
        }

        /* Footer */
        .footer {
            margin-top: 8px;
            padding-top: 6px;
            border-top: 1px solid #ddd;
            display: flex;
            justify-content: space-between;
            align-items: center;
            font-size: 8px;
            color: #666;
        }

        .footer-left {
            display: flex;
            align-items: center;
            gap: 10px;
        }

        .footer-logo {
            height: 18px;
            opacity: 0.6;
        }

        .footer-note {
            font-size: 7px;
            color: #999;
            font-style: italic;
        }
    </style>
</head>
<body>
    <div class="page">
        <!-- Header -->
        <div class="header">
            <div class="header-left">
                <img src="<%= baseUrl %>/assets/images/onzevibe_logo2.png" alt="OnzeVibe" class="logo" />
                <div>
                    <div class="header-title">Reseller Prijslijst</div>
                    <div class="header-subtitle">Alle prijzen excl. BTW</div>
                </div>
            </div>
            <div class="header-right">
                <div class="header-date"><%= formatDate(new Date()) %></div>
            </div>
        </div>

        <!-- Single big table -->
        <table class="pricing-table">
            <thead>
                <!-- Group headers row -->
                <tr>
                    <th class="qty-header" rowspan="2">Aantal</th>
                    <% groups.forEach((group, gIdx) => { %>
                    <th class="group-header" colspan="<%= group.products.length %>">
                        <%= group.name %>
                    </th>
                    <% }); %>
                </tr>
                <!-- Product headers row -->
                <tr>
                    <% groups.forEach((group, gIdx) => { %>
                        <% group.products.forEach((product, pIdx) => { %>
                        <th class="product-header">
                            <%= product.name %>
                        </th>
                        <% }); %>
                    <% }); %>
                </tr>
            </thead>
            <tbody>
                <% quantities.forEach((qty, qIdx) => { %>
                <tr>
                    <td class="qty-cell"><%= formatNumber(qty) %></td>
                    <% groups.forEach((group, gIdx) => { %>
                        <% group.products.forEach((product, pIdx) => {
                            const priceData = prices[product.id]?.[qty];
                            const isFirstInGroup = pIdx === 0;
                        %>
                        <td class="price-cell <%= isFirstInGroup ? 'group-separator' : '' %>">
                            <% if (priceData) { %>
                            <span class="price-row">
                                <span class="price-label">Inkoop</span>
                                <span class="price-value reseller"><%= formatCurrency(priceData.resellerPrice) %></span>
                            </span>
                            <span class="price-row">
                                <span class="price-label">Advies</span>
                                <span class="price-value retail"><%= formatCurrency(priceData.retailPrice) %></span>
                            </span>
                            <% } else { %>
                            <span style="color: #ccc;">-</span>
                            <% } %>
                        </td>
                        <% }); %>
                    <% }); %>
                </tr>
                <% }); %>
            </tbody>
        </table>

        <!-- Legend -->
        <div class="legend">
            <div class="legend-item">
                <div class="legend-color reseller"></div>
                <span><strong>Inkoop:</strong> Uw inkoopprijs</span>
            </div>
            <div class="legend-item">
                <div class="legend-color retail"></div>
                <span><strong>Advies:</strong> Aanbevolen verkoopprijs</span>
            </div>
            <span class="footer-note">Prijzen kunnen wijzigen. Alle prijzen excl. BTW en verzendkosten.</span>
        </div>

        <!-- Footer -->
        <div class="footer">
            <div class="footer-left">
                <img src="<%= baseUrl %>/assets/images/onzevibe_logo2.png" alt="OnzeVibe" class="footer-logo" />
                <span><strong>OnzeVibe</strong> | info@onzevibe.nl | www.onzevibe.nl</span>
            </div>
        </div>
    </div>
</body>
</html>
