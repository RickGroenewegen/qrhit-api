<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Post Your Favorite Songs!</title>
  <style>
    @media print {
      @page {
        size: A4;
        margin: 0;
      }
      body {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
      }
      img {
        image-rendering: -webkit-optimize-contrast;
        image-rendering: crisp-edges;
      }
    }
    body {
      font-family: 'Bebas Neue', 'Courier New', Courier, monospace;
      margin: 0;
      padding: 0;
      background: repeating-linear-gradient(
        135deg,
        #5FBFFF 0mm,
        #5FBFFF 10mm,
        #3F6FAF 10mm,
        #3F6FAF 20mm,
        #E56581 20mm,
        #E56581 30mm
      );
      color: #fff;
      width: 210mm;
      height: 297mm;
      min-height: 297mm;
      min-width: 210mm;
      box-sizing: border-box;
      position: relative;
      overflow: hidden;
      filter: sepia(0.12) contrast(1.08) brightness(0.98);
    }
    .poster-container {
      width: 180mm;
      height: 267mm;
      margin: 15mm auto;
      background: rgba(255,255,255,0.85);
      border-radius: 18px;
      box-shadow: 0 8px 32px 0 rgba(63, 111, 175, 0.25), 0 0 0 8px #3F6FAF inset;
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: space-between;
      padding: 24mm 18mm;
      position: relative;
      border: 6px double #E56581;
      border-top-width: 18px;
      border-bottom-width: 18px;
      box-sizing: border-box;
      /* Add a subtle paper texture overlay */
      background-image:
        url('https://www.transparenttextures.com/patterns/old-mathematics.png'),
        linear-gradient(135deg, #fff 80%, #f7f7f7 100%);
      background-blend-mode: multiply;
    }
    .poster-title {
      font-size: 64px;
      font-family: 'Bebas Neue', 'Courier New', Courier, monospace;
      font-weight: 900;
      letter-spacing: 4px;
      margin-top: 0;
      margin-bottom: 0.5mm;
      text-align: center;
      color: #E56581;
      text-shadow:
        2px 2px 0 #3F6FAF,
        0 4px 24px #0008,
        0 0 8px #fff8;
      filter: drop-shadow(0 2px 0 #fff8);
      text-transform: uppercase;
      /* border-bottom: 4px dashed #3F6FAF; */
      padding-bottom: 0.5mm;
      background: linear-gradient(90deg, #fff 60%, #E56581 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
    }
    .poster-subtitle {
      font-size: 36px;
      font-family: 'Bebas Neue', 'Courier New', Courier, monospace;
      font-weight: 900;
      letter-spacing: 4px;
      margin-top: 0;
      margin-bottom: 8mm;
      text-align: center;
      color: #E56581;
      text-shadow:
        2px 2px 0 #3F6FAF,
        0 4px 24px #0008,
        0 0 8px #fff8;
      filter: drop-shadow(0 2px 0 #fff8);
      text-transform: uppercase;
      background: linear-gradient(90deg, #fff 60%, #E56581 100%);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      background-clip: text;
      padding-bottom: 0;
    }
    .music-notes {
      position: absolute;
      top: 0; left: 0; right: 0; bottom: 0;
      pointer-events: none;
      z-index: 0;
    }
    .music-notes svg {
      position: absolute;
      opacity: 0.13;
      width: 80mm;
      height: 80mm;
      filter: blur(0.5px) sepia(0.2) contrast(1.1);
    }
    .music-notes .note1 { top: 10mm; left: 10mm; }
    .music-notes .note2 { top: 40mm; right: 20mm; }
    .music-notes .note3 { bottom: 20mm; left: 30mm; }
    .music-notes .note4 { bottom: 10mm; right: 10mm; }
    .qr-section {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin: 0 auto 0 auto;
      z-index: 2;
    }
    .qr-label {
      font-size: 28px;
      font-family: 'Bebas Neue', 'Courier New', Courier, monospace;
      font-weight: 700;
      margin-bottom: 6mm;
      color: #E56581;
      text-shadow: 0 2px 8px #3F6FAF, 0 0 2px #fff8;
      letter-spacing: 2px;
      text-transform: uppercase;
    }
    .qr-code {
      background: #fff;
      border-radius: 16px;
      padding: 4mm;
      box-shadow: 0 2px 16px 0 rgba(63,111,175,0.18), 0 0 0 4px #5FBFFF inset;
      margin-bottom: 8mm;
      display: flex;
      align-items: center;
      justify-content: center;
      border: 3px solid #3F6FAF;
    }
    .qr-code img {
      width: 70mm;
      height: 70mm;
      object-fit: contain;
      display: block;
      filter: sepia(0.2) contrast(1.1);
    }
    .poster-footer {
      font-size: 22px;
      font-family: 'Bebas Neue', 'Courier New', Courier, monospace;
      color: #3F6FAF;
      text-align: center;
      margin-top: 10mm;
      font-weight: 400;
      letter-spacing: 2px;
      text-shadow: 0 2px 8px #E56581, 0 0 2px #fff8;
      text-transform: uppercase;
    }
    .brand-domain {
      font-size: 24px;
      font-family: 'Bebas Neue', 'Courier New', Courier, monospace;
      color: #E56581;
      font-weight: 700;
      margin-top: 4mm;
      text-align: center;
      text-shadow: 0 2px 8px #3F6FAF, 0 0 2px #fff8;
      letter-spacing: 2px;
      text-transform: uppercase;
    }
    /* Add a subtle worn effect */
    .poster-container:after {
      content: "";
      pointer-events: none;
      position: absolute;
      inset: 0;
      background: url('https://www.transparenttextures.com/patterns/diamond-upholstery.png');
      opacity: 0.13;
      z-index: 10;
      mix-blend-mode: multiply;
    }
  </style>
  <!-- Vintage/retro fonts -->
  <link href="https://fonts.googleapis.com/css?family=Bebas+Neue:400,700&display=swap" rel="stylesheet">
</head>
<body>
  <div class="music-notes">
    <span class="note1">
      <svg viewBox="0 0 64 64"><text x="0" y="48" font-size="64" font-family="Arial">🎵</text></svg>
    </span>
    <span class="note2">
      <svg viewBox="0 0 64 64"><text x="0" y="48" font-size="64" font-family="Arial">🎶</text></svg>
    </span>
    <span class="note3">
      <svg viewBox="0 0 64 64"><text x="0" y="48" font-size="64" font-family="Arial">🎤</text></svg>
    </span>
    <span class="note4">
      <svg viewBox="0 0 64 64"><text x="0" y="48" font-size="64" font-family="Arial">🎧</text></svg>
    </span>
  </div>
  <div class="poster-container">
    <div style="width:100%;display:flex;justify-content:center;align-items:center;margin-bottom:10mm;">
      <img src="/assets/images/onzevibe_logo.png" alt="OnzeVibe Logo" style="max-width:90mm;max-height:32mm;object-fit:contain;">
    </div>
    <div>
      <div class="poster-title">Zwart Techniek</div>
      <div class="poster-subtitle">Top 200</div>
    </div>
    <div class="qr-section">
      <div class="qr-code">
        <img src="/assets/images/qr_example.png" alt="QR Code">
      </div>
    </div>
    <div>
   
      <div class="brand-domain">www.onzevibe.nl</div>
    </div>
  </div>
</body>
</html>
