<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Post Your Favorite Songs!</title>
  <style>
    @media print {
      @page {
        size: A4;
        margin: 0;
      }
      body {
        -webkit-print-color-adjust: exact !important;
        print-color-adjust: exact !important;
      }
      img {
        image-rendering: -webkit-optimize-contrast;
        image-rendering: crisp-edges;
      }
    }
    body {
      font-family: 'Montserrat', Arial, sans-serif;
      margin: 0;
      padding: 0;
      background: linear-gradient(135deg, #5FBFFF 0%, #E56581 100%);
      color: #fff;
      width: 210mm;
      height: 297mm;
      min-height: 297mm;
      min-width: 210mm;
      box-sizing: border-box;
      position: relative;
      overflow: hidden;
    }
    .poster-container {
      width: 180mm;
      height: 267mm;
      margin: 15mm auto;
      background: rgba(63,111,175,0.92); /* Brand secondary, slightly transparent */
      border-radius: 18px;
      box-shadow: 0 8px 32px 0 rgba(31, 38, 135, 0.37);
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: space-between;
      padding: 24mm 18mm;
      position: relative;
    }
    .poster-title {
      font-size: 48px;
      font-weight: 900;
      letter-spacing: 2px;
      margin-top: 0;
      margin-bottom: 12mm;
      text-align: center;
      color: #5FBFFF;
      text-shadow: 2px 2px 0 #E56581, 0 4px 24px #0008;
    }
    .poster-subtitle {
      font-size: 28px;
      font-weight: 600;
      margin-bottom: 10mm;
      text-align: center;
      color: #fff;
      text-shadow: 0 2px 8px #E56581;
    }
    .music-notes {
      position: absolute;
      top: 0; left: 0; right: 0; bottom: 0;
      pointer-events: none;
      z-index: 0;
    }
    .music-notes svg {
      position: absolute;
      opacity: 0.18;
      width: 80mm;
      height: 80mm;
    }
    .music-notes .note1 { top: 10mm; left: 10mm; }
    .music-notes .note2 { top: 40mm; right: 20mm; }
    .music-notes .note3 { bottom: 20mm; left: 30mm; }
    .music-notes .note4 { bottom: 10mm; right: 10mm; }
    .qr-section {
      display: flex;
      flex-direction: column;
      align-items: center;
      margin: 0 auto 0 auto;
      z-index: 2;
    }
    .qr-label {
      font-size: 22px;
      font-weight: 600;
      margin-bottom: 6mm;
      color: #E56581;
      text-shadow: 0 2px 8px #0008;
    }
    .qr-code {
      background: #fff;
      border-radius: 16px;
      padding: 12mm;
      box-shadow: 0 2px 16px 0 rgba(0,0,0,0.18);
      margin-bottom: 8mm;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .qr-code img {
      width: 60mm;
      height: 60mm;
      object-fit: contain;
      display: block;
    }
    .poster-footer {
      font-size: 18px;
      color: #fff;
      text-align: center;
      margin-top: 10mm;
      font-weight: 400;
      letter-spacing: 1px;
      text-shadow: 0 2px 8px #E56581;
    }
    .brand-domain {
      font-size: 20px;
      color: #5FBFFF;
      font-weight: 700;
      margin-top: 4mm;
      text-align: center;
      text-shadow: 0 2px 8px #0008;
    }
  </style>
  <!-- Optionally include a Google Fonts link for Montserrat -->
  <link href="https://fonts.googleapis.com/css?family=Montserrat:400,600,900&display=swap" rel="stylesheet">
</head>
<body>
  <div class="music-notes">
    <span class="note1">
      <svg viewBox="0 0 64 64"><text x="0" y="48" font-size="64" font-family="Arial">🎵</text></svg>
    </span>
    <span class="note2">
      <svg viewBox="0 0 64 64"><text x="0" y="48" font-size="64" font-family="Arial">🎶</text></svg>
    </span>
    <span class="note3">
      <svg viewBox="0 0 64 64"><text x="0" y="48" font-size="64" font-family="Arial">🎤</text></svg>
    </span>
    <span class="note4">
      <svg viewBox="0 0 64 64"><text x="0" y="48" font-size="64" font-family="Arial">🎧</text></svg>
    </span>
  </div>
  <div class="poster-container">
    <div>
      <div class="poster-title">Share Your Favorite Songs!</div>
      <div class="poster-subtitle">
        Scan the QR code<br>
        and post your top tracks.<br>
        <span style="font-size:20px;color:#E56581;">Let the music speak!</span>
      </div>
    </div>
    <div class="qr-section">
      <div class="qr-label">Scan to Post</div>
      <div class="qr-code">
        <img src="<%= appDomain %>/public/qr/poster/<%= posterId %>.png" alt="QR Code">
      </div>
    </div>
    <div>
      <div class="poster-footer">
        Join the vibe. Inspire others. <br>
        <span style="font-size:22px;font-weight:600;color:#E56581;">#MyTopSongs</span>
      </div>
      <div class="brand-domain"><%= appDomain %></div>
    </div>
  </div>
</body>
</html>
